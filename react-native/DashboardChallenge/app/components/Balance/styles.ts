import { StyleSheet } from 'react-native';

export  const styles = StyleSheet.create({
  balanceContainer: {
    padding: 20,
    backgroundColor: '#fff',
    marginVertical: 10,
  },
  label: {
    fontSize: 16,
    color: '#666',
  },
  balance: {
    fontSize: 24,
    fontWeight: 'bold',
  },
}); 

export default {};